

# Jwt-auth (Admin Panels)

Полноценное клиент-серверное приложение с авторизацией на основе JWT.
Проект реализует админ-панели для управления бойцами, боями и татами.

## 🚀 Стек технологий

### Frontend

* **React**
* **TypeScript**
* **MobX** (управление состоянием)
* **Vite** (быстрая сборка)
* Axios (HTTP-запросы)
* React Router

### Backend

* **Node.js**
* **Express**
* **MongoDB**
* Mongoose
* JWT (JSON Web Token)
* bcrypt (хеширование паролей)
* dotenv

---

## 🔐 Основные возможности

* JWT-аутентификация и защита маршрутов
* Разделение ролей (админ-доступ)
* CRUD:

  * Бойцы
  * Бои
  * Татами
* Глобальное состояние через MobX
* Валидация данных на сервере
* Обработка ошибок
* REST API архитектура
* Чистая модульная структура проекта

---

## 📂 Архитектура проекта

Проект не является «простым CRUD».
Он построен по принципам разделения ответственности и масштабируемости.

```
client/   — фронтенд (React + TypeScript + MobX + Vite)
server/   — backend (Express + MongoDB)
```

### Сервер

* controllers/
* models/
* routes/
* middleware/
* services/

### Клиент

* pages/
* components/
* store/
* api/
* hooks/

---

## 📦 Установка

### 1. Клонировать репозиторий

```bash
git clone https://github.com/danilturko4-crypto/Jwt-auth-admin-panels.git
cd Jwt-auth-admin-panels
```

### 2. Установить зависимости

```bash
cd server
npm install

cd ../client
npm install
```

---

## ⚙️ Переменные окружения

Создайте `.env` в папке `server/`:

```
PORT=5000
DB_URL=mongodb://localhost:27017/jwt-auth
JWT_SECRET=your_super_secret_key
```

---

## 🧪 Запуск в режиме разработки

### Сервер

```bash
cd server
npm run dev
```

### Клиент

```bash
cd client
npm run dev
```

---

## 🏗 Сборка для production

```bash
cd client
npm run build

cd ../server
npm start
```


